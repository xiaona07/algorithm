/**
 * 递归字符串匹配
 * @param str 字符串
 * @param rule 规则 * ?
 * @returns {number} 匹配长度
 */
function recursiveMatch(str, rule) {
    let matchIndex = -1
    str.charAt(0)
}

console.log(recursiveMatch("fashghdssssd", "f*d"))
//贪婪的时候输出12，非贪婪的时候输出7

